// Calibration lines. Forward hook per SYNTHESIS.md section 7.6. The current cohort does not require calibration; the file is intentionally empty. Entries can be added later without modifying logic files.

import type { CalibrationLine } from '../types';

export const calibrationLines: CalibrationLine[] = [];
