// Closing line construction. This module is retired.
// The closing-lines footer has been removed. Former closing observations now render as:
// - capacity_strain/stopped_expecting → direction cards (closing_observation)
// - between_shapes/mid_process → Current Shape panel (life_context_panel)
// - phantom → redundant with card state caption

export function computeClosingLines(): [] {
  // All closing lines have been relocated or are redundant.
  return [];
}
